<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/splash_nav_graph"
    app:startDestination="@id/splashFragment">

    <fragment
        android:id="@+id/splashFragment"
        android:name="com.github.lol4fun.features.splash.view.SplashFragment"
        android:label="SplashFragment"
        tools:layout="@layout/fragment_splash">
        <action
            android:id="@+id/splash_to_home"
            app:destination="@id/mainActivity"
            app:launchSingleTop="true"
            app:popUpTo="@id/splashFragment"
            app:popUpToInclusive="true" />
        <action
            android:id="@+id/splash_to_nickname"
            app:destination="@id/nicknameFragment"
            app:launchSingleTop="true"
            app:popUpTo="@id/splashFragment"
            app:popUpToInclusive="true" />
    </fragment>

    <fragment
        android:id="@+id/nicknameFragment"
        android:name="com.github.lol4fun.features.nickname.view.NicknameFragment"
        android:label="NicknameFragment"
        tools:layout="@layout/fragment_nickname">
        <action
            android:id="@+id/nickname_to_home"
            app:destination="@id/mainActivity"
            app:launchSingleTop="true"
            app:popUpTo="@id/nicknameFragment"
            app:popUpToInclusive="true" />
    </fragment>
    <activity
        android:id="@+id/mainActivity"
        android:name="com.github.lol4fun.features.main.view.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
</navigation>